import 'package:flutter/material.dart';

class AddActivitiesPageContent extends StatelessWidget {
  const AddActivitiesPageContent({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('dwa'),
    );
  }
}